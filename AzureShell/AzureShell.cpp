#include <iostream>
#include <string>
#include <Windows.h>
#include <filesystem>
int main()
{
	using namespace std;
	//get directory of where the shell is running
	string dir = 

    
    cout << "Azure Shell (c) Azureian 2023\n";
    while (true)
    {
		string command;
		cout << "AS > ";
		cin >> command;
		if (command == "exit")
		{
			break;
		}
		else
		{
			cout << "Unknown command: " << command << endl;
		}
	}
}